This project implements the classic Tic Tac Toe game with a C server and a C client.  
- To compile, do *gcc -o server minor4server.c* and *gcc -o client minor4client.c*  
- To run, do *./server* and *./client*
