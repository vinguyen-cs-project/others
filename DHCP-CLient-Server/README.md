This project mimics message exchange between a DHCP client and server.

- To compile the program:  
A makefile was created, so just run 'make'
- To run the program:  
run command ./dserver <port> and ./dclient <port>
